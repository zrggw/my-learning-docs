# 简单 Agent 实现 · 15 分钟白板分享稿（无 PPT / 现场手写）

**讲什么**：一个最小可用的 LLM Agent 由哪几部分构成、Agent Loop 如何驱动它、代码怎么写出来，以及抽象成框架之后范式差异落在哪里。

> **主题**：简单 Agent 的实现原理与代码结构
> **范围**：以单 Agent 最小闭环为主线——PEAS 角色划分、Agent Loop、Thought-Action-Observation 协议、工具注册与主循环代码、抽象基类与工具系统；文末以对照表简述 ReAct、Plan-and-Solve、Reflection。**不涉及**大语言模型的内部原理（注意力、训练、推理优化）与智能体发展历史。
> **形式**：白板手写，8 块板书；开头的「Agent ↔ 环境」循环图与文末范式对照表投屏，其余内容现场书写。
> **标称时长**：15 分钟；**净演讲 14:50**（0:00–14:50）
> **素材来源**：《Hello-Agents》本地仓库 `D:\code\hello-agents\docs`——第一章「初识智能体」1.2/1.3 节、第七章「构建你的 Agent 框架」7.3/7.4 节、第四章「智能体经典范式构建」4.2–4.4 节。图、表按原文章节号标注。
> **代码出处**：板 5、板 6 的口播要点取自原文 1.3 节「5 分钟实现第一个智能体」的完整实现（`AGENT_SYSTEM_PROMPT`、`available_tools` 字典、`prompt_history` 主循环、三段正则解析），函数名、参数与正则逐字保留；板 7 取自原文 7.3/7.4 节的框架接口。板书为关键词摘录，现场书写不要求逐字复现源码。
> **自撰内容**：讲前准备、时间分配、板书可书写性核算、逐板落点与过渡句，为本次分享编排，非原文内容。

---

## 0. 讲前准备

**白板分区（3 栏，从左到右）**

| 栏位 | 用途 | 是否保留全场 |
|---|---|---|
| 左栏（约 1/4 宽） | Agent Loop 骨架（含「控制器 / 执行器 / 传感器」三层标注） | **保留，不擦** |
| 中栏（约 1/2 宽） | 当块板书：PEAS 对照、代码、协议 | 逐块擦写 |
| 右栏（约 1/4 宽） | 红色易错点、绿色结论 | 保留，不擦 |

**颜色约定**：黑＝结构与流程；蓝＝定义、接口名、代码；红＝限制与易错点；绿＝结论与落地建议。每块换色不超过 2 次。

**必须提前画好的骨架（不占现场书写时间）**

| 项目 | 内容 | 预计耗时 |
|---|---|---|
| 左栏骨架 | Agent Loop 五元骨架：`环境 → 传感器 → 思考 → 行动 → 环境`（含 Observation 回边箭头），标注控制器／执行器／传感器三层 | 约 40 s |
| 右栏表头 | 「易错点」与「结论」两个分区表头 | 约 6 s |
| 投影准备 | 打开 `images/agent-loop.png`（原文图 1.5）与范式对照表（原文第四章 4.2–4.4），确认投屏可读 | 约 20 s |

**图与素材取舍**

- **只投图 1.5，不投代码**：代码由现场「从零搭起来」的叙事骨架承担；投屏代码会让人只看屏幕不看白板。
- 图 1.5 中 `Perception` 对应 PEAS 的 Sensors，`Action` 对应 Actuators；讲板 2 时用蓝笔在投影旁补注这两个词。
- 原表 1.2（PEAS 描述）不投屏，改为板 2 的手写对照；现场画一张表格约需 35 s，超出一块正文的板书预算。

---

## 1. 时间分配表

| 板 | 内容 | 时间 | 时长 | 超时了怎么砍 |
|---|---|---|---|---|
| 1 | 开场：环境里跑的循环 | 0:00–0:45 | 0:45 | 只留第一行骨架 |
| 2 | PEAS：传感器与执行器的位置 ★不可砍 | 0:45–2:45 | 2:00 | 环境特性转口播 |
| 3 | Agent Loop：一个循环、三个约束 ★不可砍 | 2:45–4:45 | 2:00 | 砍「原始数据转自然语言」 |
| 4 | 交互协议：三段式文本结构 | 4:45–6:45 | 2:00 | 砍三个理由 |
| 5 | 代码：主循环与行动解析（代码板 ①） ★不可砍 | 6:45–9:20 | 2:35 | 砍正则截断一段 |
| 6 | 代码：工具注册与回复质量（代码板 ②） | 9:20–11:20 | 2:00 | 砍运行案例 |
| 7 | 框架化：抽象基类与工具系统 | 11:20–13:20 | 2:00 | 砍范式选型顺序 |
| 8 | 收尾：结论与落地 | 13:20–14:50 | 1:30 | 只留第一条结论 |

**累计**：0:00 → 14:50，与净演讲时长一致。

**机动（时间海绵）**：板 4 的三个理由、板 6 的运行案例、板 7 的选型顺序，合计约 1 分 30 秒。**首选砍板 7 的选型顺序**，其次砍板 4 的三个理由。

**优先级**：**不可砍**——板 2（PEAS 角色划分）、板 3（Agent Loop 主线）、板 5（主循环代码，本次重点）；**首选砍**——板 7、板 4。

---

## 2. 逐板脚本

### 板 1 ｜ 开场：环境里跑的循环 ⏱ 0:00–0:45　板书 ≈ 18 s

**🖊 板书**

```text
环境 ─→ 传感器 ─→ 思考 ─→ 行动 ─┐
  ↑                            │
  └──── 观察（Observation）←────┘
```

**🖼 参考图**（开场即投屏）

![图 1.5 智能体与环境交互的基本循环](images/agent-loop.png)

- 图源：原文图 1.5「智能体与环境交互的基本循环」；文件 `images/agent-loop.png`。
- 用法：投屏后对照左栏骨架，指出 `Perception` 与 `Action` 两个接口的位置。

**🎤 口播要点**

- 本次主题是**一个最小可用的 Agent 怎么实现**：只讲 agent 这一层的机制与代码结构，不涉及大语言模型内部原理。
- 一句话主线：**Agent 的本质是一个循环**——通过传感器读取环境状态，交给模型思考决策，再通过执行器把行动施加回环境。

**🔗 过渡句**："先明确这个循环里谁是环境、谁是执行器、谁是传感器——不定义清楚，后面的代码就没有边界。"

**🧭 落点**：Agent 的本质是「感知—思考—行动」的循环，不是一次性的问答。

---

### 板 2 ｜ PEAS：传感器与执行器的位置 ⏱ 0:45–2:45　板书 ≈ 27 s

**🖊 板书**（中栏）

```text
传感器 ← 读用户输入 / 解析 API 返回
执行器 → 调用工具函数 / 向界面输出
```

**🖼 参考图**：对照投屏图 1.5，用蓝笔补注 `Perception = Sensors`、`Action = Actuators`。

**🎤 口播要点**

- 描述任务环境用 **PEAS 模型**：**P**erformance（性能度量）、**E**nvironment（环境）、**A**ctuators（执行器）、**S**ensors（传感器）。四个格子填完，Agent 的职责边界就定了；原文表 1.2 以智能旅行助手为例给了完整填法。
- **传感器一侧就是感知**：读取用户输入的自然语言、解析 API 返回的 JSON 或 HTML，并整理成模型能读的自然语言。**执行器一侧就是行动**：调用工具函数、向界面输出格式化文本。图 1.5 里的 Perception 与 Action 就是这两者。
- 工程含义：设计一个 Agent，主要工作是把这四个格子填满；模型本身接触不到网络与文件系统，一切环境影响都必须经由执行器。环境特性——部分可观察、结果随机、存在其他行动者、序贯且动态——共同要求循环足够快并具备记忆。

**🔗 过渡句**："职责划分清楚了，接下来看这个循环具体怎么转。"

**🧭 落点**：PEAS 把环境描述变成填空题，传感器对应感知、执行器对应行动，Agent 与环境的耦合只发生在这两个接口上。

---

### 板 3 ｜ Agent Loop：一个循环、三个约束 ⏱ 2:45–4:45　板书 ≈ 27 s

**🖊 板书**（中栏 ①）

```text
for i in range(N):        ← 上限
  拼历史 → 调模型 → 解析行动
  Finish[...] → break     ← 出口
```

**🖼 参考图**：沿用板 1 已投屏的图 1.5，指认 `Thought` 框内并列的 `Planning` 与 `Tool Selection` 两个子框。

**🎤 口播要点**

- **Agent Loop**：感知、思考、行动、观察构成闭环。思考阶段内部还分两步——规划（Planning）与工具选择（Tool Selection）：前者决定先做什么，后者决定用哪个工具、传什么参数；在图 1.5 里是 Thought 框内并列的两个子框。
- 三个约束决定这个循环能否上生产：**最大循环次数**防止无限循环并控制成本与延迟；**明确的终止条件**（模型输出 `Finish[最终答案]`）让循环退出；**原始数据转自然语言**是传感器层的职责——把天气 API 返回的 JSON 整理成「北京当前天气:Sunny，气温26摄氏度」。
- 关键点：**模型不知道当前是第几轮**。轮次上限是宿主代码的职责——每次迭代都把完整历史拼成一条提示词交给模型，模型只负责生成下一步，其余全部由代码控制。
- 易错点：演示代码常省略轮次上限或终止判断，模型不肯收尾时会一直跑下去，耗时与费用都不可控。

**🔗 过渡句**："循环由代码驱动，那么模型每一步要输出什么，代码才解析得动？这就是协议。"

**🧭 落点**：Agent Loop 是宿主代码里的 `for` 循环；模型只做单步决策，轮次上限与终止条件由代码强制。

---

### 板 4 ｜ 交互协议：三段式文本结构 ⏱ 4:45–6:45　板书 ≈ 26 s

**🖊 板书**（中栏 ②）

```text
Thought → Action → Observation
Action: get_weather(city="北京")
三个理由：模型无关 · 可解析 · 可留痕
```

**🎤 口播要点**

- 协议三段：模型输出 `Thought`（推理快照）与 `Action`（具体动作），Action 在实现上是**函数调用形式**；环境返回的结果由宿主代码包装成 `Observation` 回填。三者构成完整对话历史，也就是下一轮的输入。
- 这个结构化文本称为**交互协议（Interaction Protocol）**：模型输出不再是单一自然语言回复，而是「推理过程 + 最终决策」两段，使代码可以稳定解析。
- 采用文本协议而不是 JSON 或原生函数调用接口，理由有三点：**模型无关**——凡兼容 OpenAI 接口规范的服务（OpenAI、Azure、Ollama、vLLM）都能直接运行；**可解析**——正则即可稳定提取函数名与参数；**可留痕**——整条 `Thought-Action-Observation` 串就是可打印的日志。
- 格式约束必须由系统提示词显式声明：一次只输出一对 Thought-Action、Action 必须写在同一行、信息足够后必须用 `Finish[最终答案]` 结束。

**🔗 过渡句**："协议定了，下面就把它落到代码上——这是本次分享的重点。"

**🧭 落点**：Thought-Action-Observation 是宿主代码与模型之间的文本约定，它让模型意图变成代码可解析、可执行、可回溯的步骤。

---

### 板 5 ｜ 代码：主循环与行动解析（代码板 ①） ⏱ 6:45–9:20　板书 ≈ 16 s

**🖊 板书**

```text
tools = {"get_weather": get_weather}
out = llm.generate(拼接历史)
a = re.search(r"Action: (.*)").group(1)
```

**🎤 口播要点**

- 前置两件事：**系统提示词**说明角色、可用工具与输出格式；**工具字典**把工具名映射到函数对象——`available_tools = {"get_weather": get_weather, "get_attraction": get_attraction}`，主循环据此调用。
- 初始化只有一个列表：`prompt_history = [f"用户请求: {user_prompt}"]`。每轮把 `"\n".join(prompt_history)` 作为提示词传入——**模型侧没有状态，历史必须每次重新拼接**，这是该架构最容易理解错的一点。
- 解析分三步，都基于正则：先用 `re.search(r'(Thought:.*?Action:.*?)(?=\n\s*(?:Thought:|Action:|Observation:)|\Z)', llm_output, re.DOTALL)` 截断多余输出，只保留第一对 Thought-Action；再用 `re.search(r"Action: (.*)", llm_output, re.DOTALL)` 取行动串，**取不到时不抛异常**，而是把一句错误说明作为 Observation 回填，下一轮模型据此自我纠正。
- 第三步：`re.search(r"(\w+)\(", action_str)` 取函数名，`re.findall(r'(\w+)="([^"]*)"', args_str)` 取参数字典，随后 `available_tools[tool_name](**kwargs)` 展开调用；`Finish[...]` 分支在工具查找之前判断，取方括号内文本作为最终答案并退出循环。
- 易错点：参数解析依赖引号，提示词必须强制 `arg_name="arg_value"` 形式；`get_weather(city=北京)` 这类输出会直接解析失败。**提示词里的格式约束与代码里的解析规则必须成对设计。**

**🔗 过渡句**："主循环跑通了，还有两处决定这套代码能不能复用：工具怎么注册、工具为什么不抛异常。"

**🧭 落点**：代码实现的核心是「工具字典 + 正则解析 + 观察回填 + 最大轮次」，四者构成可运行的最小实现。

---

### 板 6 ｜ 代码：工具注册与回复质量（代码板 ②） ⏱ 9:20–11:20　板书 ≈ 25 s

**🖊 板书**

```text
obs = tools[fn](**kw)
history += [out, "Observation: " + obs]
工具 = 普通函数，返回文本不抛异常
```

**🎤 口播要点**

- **工具就是普通 Python 函数**，不继承任何基类；注册只有两步——写函数、放进字典，解析与调用都由主循环负责。
- 函数签名的类型标注要与提示词里写给模型的描述一致（`get_weather(city: str)`）：提示词是给模型看的规格说明，签名是代码里的真实入口。两者不一致就会出现「模型按提示词调用、代码里没有这个参数」的失败。
- **工具不抛异常、只返回文本**：网络失败返回 `"错误:查询天气时遇到网络问题 - {e}"`，城市名无效返回解析失败说明。这个观察值进入下一轮，模型据此换参数或换工具——工具的错误信息是模型可见的输入，不是只给开发者看的日志。
- 运行案例（原文 1.3.4 节实测输出）：第一轮 `Action: get_weather(city="北京")`，观察为 `北京当前天气:Sunny，气温26摄氏度`；第二轮 `Action: get_attraction(city="北京", weather="Sunny")`；第三轮 `Action: Finish[...]` 收尾。由此体现四项基本能力：任务分解、工具调用、上下文理解、结果合成。

**🔗 过渡句**："单文件脚本到这里就完整了。多写几种范式之后会发现重复代码很多，所以需要抽象一层。"

**🧭 落点**：工具是普通函数加一个字典；工具返回的错误文本是模型可用的信息，工具集的质量直接决定 Agent 的能力边界。

---

### 板 7 ｜ 框架化：抽象基类与工具系统 ⏱ 11:20–13:20　板书 ≈ 17 s

**🖊 板书**（中栏 ④）

```text
Agent(ABC) ──run()──→ 具体 Agent
Message：统一 role 与转换
```

**📌 对照表**（投屏，原文第四章 4.2–4.4 节）

| 范式 | 循环结构 | 适用任务 |
|---|---|---|
| `SimpleAgent` | 一问一答，无工具循环 | 知识性问答、文本处理 |
| `ReAct` | Thought ↔ Action 交替，观察回填 | 需要外部信息的任务（检索、计算、调接口） |
| `Plan-and-Solve` | 先规划全部步骤，再逐步执行并携带历史 | 可预先分解的多步任务 |
| `Reflection` | 执行 → 自我批评 → 再执行 | 对质量要求高、可迭代改进的任务（如代码生成） |

**🎤 口播要点**

- 框架化的核心动作是**把 `run` 定为统一入口**：`Agent` 继承 `abc.ABC`，`run` 加 `@abstractmethod`，任何 Agent 范式都必须实现它；名称、LLM 实例、系统提示词、配置统一由基类构造函数接收，历史记录管理（`add_message` / `clear_history` / `get_history`）也上提到基类。基类里没有 `run` 的实现——它对应各范式不同的循环。
- **`Message` 统一消息格式**：`role` 用 `typing.Literal` 限制为 `"user" / "assistant" / "system" / "tool"` 四种，与 OpenAI API 规范一一对应；`to_dict()` 只输出 `role` 与 `content`，`timestamp`、`metadata` 留给内部使用，即「对内丰富，对外兼容」。
- 工具系统有两种写法：函数字典适合演示与小型项目，零抽象、直接调用；工具基类加注册表适合集成，可统一元信息、参数校验与执行入口。
- 三种经典范式的差异**不在模型，而在循环结构**：ReAct 多了「工具调用—观察回填」的往返；Plan-and-Solve 把循环拆成规划与执行两个阶段；Reflection 增加一层自我评价，以调用次数换质量。选型顺序：能一次答对 → `SimpleAgent`；需要外部信息 → `ReAct`；可预先分解为多步 → `Plan-and-Solve`；质量优先 → `Reflection`。

**🔗 过渡句**："机制、协议、代码、抽象四层都过了一遍，收拢成两条结论。"

**🧭 落点**：框架化的收益是统一入口、历史管理与工具复用，各范式的差异只体现在循环结构与提示词上。

---

### 板 8 ｜ 收尾：结论与落地 ⏱ 13:20–14:50　板书 ≈ 21 s

**🖊 板书**（右栏，保留）

```text
PEAS 划边界，协议可解析
循环控轮次，工具表定能力
```

**🎤 口播要点**

- 第一条结论：**PEAS 划边界，协议可解析**——传感器与执行器定义了 Agent 与环境之间的唯一接口，Thought-Action-Observation 让模型输出变成可解析的步骤。
- 第二条结论：**循环控轮次，工具表定能力**——Agent Loop 是宿主代码的 `for` 循环，模型只做单步决策；轮次上限、终止条件、记忆回填都由代码负责，这段控制逻辑的质量决定稳定性。
- 落地建议：实现一个最小 Agent 只需四件事——系统提示词、工具字典、正则解析、观察回填，约一百行代码即可跑通；再用抽象基类把 `run` 统一，就能在同一框架下切换范式。

**🔗 过渡句**："以上是本次分享的内容，下面是提问时间。"

**🧭 落点**：把 Agent 当成「一个循环 + 一份协议 + 一张工具表」，实现与选型都会变得清晰。

---

## 3. 同事提问与回答（现场记录）

> 现场记录本环节同事提出的问题与回答，便于事后整理与补充。
